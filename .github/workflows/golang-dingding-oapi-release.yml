name: golang-dingding-oapi-release
on:
  push:
    paths:
      - "golang/dingding-oapi/**"
      - ".github/workflows/golang-dingding-oapi-release.yml"
    branches:
      - "main"
jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@v2

      - name: 🚀 Release
        uses: gsmlg/semantic-release-action@v3
        with:
          release_name: dingding-oapi
          working-directory: 'golang/dingding-oapi'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

