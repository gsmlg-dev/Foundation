name: nodejs-eslint-config-validate-and-release
on:
  push:
    paths:
      - 'nodejs/packages/eslint-config/**'
      - '.github/workflows/nodejs-eslint-config-validate-and-release.yml'
    branches:
      - 'main'
jobs:
  main:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        node: [14, 16]
    runs-on: ${{ matrix.os }}
    steps:
      - name: 🛑 Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.9.1
        with:
          access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v2

      - name: ⎔ Setup node
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node }}

      - name: 📥 Download deps
        working-directory: 'nodejs/packages/eslint-config'
        run: npm install

      - name: ▶️ Run validate script
        working-directory: 'nodejs/packages/eslint-config'
        run: npm run validate

      - name: ⬆️ Upload coverage report
        uses: codecov/codecov-action@v1
        with:
          directory: 'nodejs/packages/eslint-config/coverage/'

  release:
    needs: main
    runs-on: ubuntu-latest
    if: ${{ github.repository == 'gsmlg-dev/Foundation' && contains('refs/heads/main',github.ref) && github.event_name == 'push' }}
    steps:
      - name: 🛑 Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.6.0
        with:
          access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v2

      - name: ⎔ Setup node
        uses: actions/setup-node@v2
        with:
          node-version: 16

      - name: 📥 Download deps
        working-directory: 'nodejs/packages/eslint-config'
        run: npm install

      - name: 🏗 Run install semantic-release
        working-directory: 'nodejs/packages/eslint-config'
        run: npm install --save-dev semantic-release-monorepo semantic-release

      - name: 🚀 Release
        working-directory: 'nodejs/packages/eslint-config'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx semantic-release --extends semantic-release-monorepo
